[project]
name = "agent"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
    "langchain[ollama]>=1.2.12",
    "langgraph>=1.1.2",
    "rich>=14.3.3",
]
